<Page
    x:Class="ChyguiSlide.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:ChyguiSlide.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </Page.Resources>

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <TextBlock Text="Логи приложения" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,12"/>
            <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" 
                       Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                       Visibility="{x:Bind ViewModel.StatusMessage, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}"
                       Margin="0,0,0,8"/>
        </StackPanel>

        <!-- Logs content -->
        <ScrollViewer Grid.Row="1" Padding="20">
            <ItemsControl ItemsSource="{x:Bind ViewModel.Logs, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" 
                                   FontFamily="Consolas,Courier New"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,4"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer with buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Padding="20"
                    Spacing="12">
            <Button Content="Очистить" 
                    Command="{x:Bind ViewModel.ClearCommand}"
                    Style="{StaticResource AccentButtonStyle}"/>
            <Button Content="Копировать" 
                    Command="{x:Bind ViewModel.CopyCommand}"/>
            <Button Content="Сохранить в файл" 
                    Command="{x:Bind ViewModel.SaveCommand}"/>
        </StackPanel>
    </Grid>
</Page>
